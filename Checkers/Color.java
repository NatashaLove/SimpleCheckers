package Checkers;

// if enum in a separate class- put ntations as though it is class itself (dot) 
public enum Color {RED, BLACK};

// will be like:

//  r1.set(Color.RED);

//class-Piece.enum
	//public Color checkColor (int x, int y){
	//	return board[x][y].getColor();
	//}